multioutreg.time_series.ts_dynamic_fit.src.visualize
====================================================

.. py:module:: multioutreg.time_series.ts_dynamic_fit.src.visualize


Functions
---------

.. autoapisummary::

   multioutreg.time_series.ts_dynamic_fit.src.visualize.get_predictions
   multioutreg.time_series.ts_dynamic_fit.src.visualize.plot_predictions_vs_actual
   multioutreg.time_series.ts_dynamic_fit.src.visualize.plot_residuals
   multioutreg.time_series.ts_dynamic_fit.src.visualize.plot_acf_pacf_generic
   multioutreg.time_series.ts_dynamic_fit.src.visualize.plot_model_specific_diagnostics
   multioutreg.time_series.ts_dynamic_fit.src.visualize.visualize_model


Module Contents
---------------

.. py:function:: get_predictions(model: Any, df: pandas.DataFrame, target_col: str, forecast_steps: int = 0) -> pandas.Series

   Attempts to obtain predictions from the given model for the input dataframe.
   Extend this with model-specific logic as needed.

   :param model: The forecasting model instance.
   :param df: The input data.
   :type df: pd.DataFrame
   :param target_col: The name of the target column.
   :type target_col: str
   :param forecast_steps: Number of forecast steps for the model, if used.
   :type forecast_steps: int

   :returns: Predicted values indexed like df.
   :rtype: pd.Series


.. py:function:: plot_predictions_vs_actual(df: pandas.DataFrame, preds: Union[numpy.ndarray, pandas.Series], target_col: str) -> None

   Plot actual vs predicted values.

   :param df: The input data.
   :type df: pd.DataFrame
   :param preds: Predicted values to plot.
   :type preds: np.ndarray or pd.Series
   :param target_col: Target variable column name.
   :type target_col: str

   :returns: None


.. py:function:: plot_residuals(df: pandas.DataFrame, preds: Union[numpy.ndarray, pandas.Series], target_col: str) -> pandas.Series

   Plot residuals over time and as a histogram.

   :param df: Input data.
   :type df: pd.DataFrame
   :param preds: Predictions from the model.
   :type preds: np.ndarray or pd.Series
   :param target_col: Name of the target column.
   :type target_col: str

   :returns: Residuals (actual - predicted).
   :rtype: pd.Series


.. py:function:: plot_acf_pacf_generic(residuals: Union[numpy.ndarray, pandas.Series], lags: int = 40) -> None

   Plots ACF/PACF for residuals (works on any model).

   :param residuals: Residuals from the model.
   :type residuals: np.ndarray or pd.Series
   :param lags: Number of lags to display.
   :type lags: int

   :returns: None


.. py:function:: plot_model_specific_diagnostics(model: Any) -> None

   If available, shows model native diagnostic plots (e.g., ARIMA .plot_diagnostics()).

   :param model: Model instance, possibly statsmodels.

   :returns: None


.. py:function:: visualize_model(model: Any, df: pandas.DataFrame, target_col: Optional[str] = None, forecast_steps: int = 12, lags: int = 40) -> None

   Main entry point: visualize model results for any time series model.

   :param model: Time series forecasting model.
   :param df: Input data including target and features.
   :type df: pd.DataFrame
   :param target_col: The target variable column name. Defaults to first column in df.
   :type target_col: str, optional
   :param forecast_steps: Number of future steps to forecast and plot.
   :type forecast_steps: int
   :param lags: Number of lags for residual ACF/PACF.
   :type lags: int

   :returns: None


