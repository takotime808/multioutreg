multioutreg.surrogates.gpx_smt
==============================

.. py:module:: multioutreg.surrogates.gpx_smt

.. autoapi-nested-parse::

   GPX surrogate — Rust-accelerated Kriging via SMT/egobox.

   Wraps the ``GPX`` model from the SMT Surrogate Modeling Toolbox, which
   reimplements sklearn-style Kriging (KRG) in compiled Rust via the egobox
   library.  The probabilistic model is identical to a standard GP/Kriging
   but training and prediction are 10–100× faster for the same dataset size.

   Installation
   ------------
       pip install smt[gpx]

   If the optional dependency is not installed this module defines placeholder
   classes that raise ``ImportError`` at instantiation so the rest of the
   package continues to work without it.



Attributes
----------

.. autoapisummary::

   multioutreg.surrogates.gpx_smt._GPX_AVAILABLE


Classes
-------

.. autoapisummary::

   multioutreg.surrogates.gpx_smt._GPXEstimator
   multioutreg.surrogates.gpx_smt.GPXSurrogate


Functions
---------

.. autoapisummary::

   multioutreg.surrogates.gpx_smt._check_available


Module Contents
---------------

.. py:data:: _GPX_AVAILABLE
   :value: True


.. py:function:: _check_available()

.. py:class:: _GPXEstimator(corr='squar_exp', poly='constant', n_start=10)

   Bases: :py:obj:`sklearn.base.BaseEstimator`, :py:obj:`sklearn.base.RegressorMixin`


   Single-output GPX estimator (sklearn-compatible, used by GridSearchCV).


   .. py:attribute:: corr
      :value: 'squar_exp'



   .. py:attribute:: poly
      :value: 'constant'



   .. py:attribute:: n_start
      :value: 10



   .. py:method:: fit(X, y)


   .. py:method:: predict(X, return_std=False)


.. py:class:: GPXSurrogate(corr='squar_exp', poly='constant', n_start=10)

   Bases: :py:obj:`multioutreg.surrogates.conformal_mixin.ConformalMixin`


   Rust-accelerated Kriging surrogate via SMT/egobox GPX.

   Fits one :class:`_GPXEstimator` per output column using the SMT ``GPX``
   backend, which compiles the Kriging likelihood and gradient to native
   Rust for a 10–100× speedup over sklearn's ``GaussianProcessRegressor``
   at the same O(n³) asymptotic cost.

   Requires ``pip install smt[gpx]``.  The dependency is optional; if not
   installed an ``ImportError`` is raised at instantiation time.

   :param corr: Correlation function: ``"squar_exp"`` (RBF), ``"abs_exp"``,
                ``"matern52"``, or ``"matern32"``.
   :type corr: str
   :param poly: Regression/trend term: ``"constant"``, ``"linear"``, or
                ``"quadratic"``.
   :type poly: str
   :param n_start: Number of multistart optimizer runs for hyperparameter MLE.
   :type n_start: int


   .. py:attribute:: corr
      :value: 'squar_exp'



   .. py:attribute:: poly
      :value: 'constant'



   .. py:attribute:: n_start
      :value: 10



   .. py:method:: fit(X, Y)


   .. py:method:: predict(X, return_std=False)


   .. py:method:: _conformal_point_predict(X)


